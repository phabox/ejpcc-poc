spring:
  datasource:
    url: jdbc:postgresql://accountings-db:5432/accountings
    
  kafka:
    bootstrap-servers: kafka:9092
    consumer:
      bootstrap-servers: kafka:9092
    producer:
      bootstrap-servers: kafka:9092

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8090/eureka/