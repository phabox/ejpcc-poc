spring:
  datasource:
    url: jdbc:postgresql://winningnumbers-db:5432/winningnumbers
    
  kafka:
    bootstrap-servers: kafka:9092
    consumer:
      bootstrap-servers: kafka:9092
    producer:
      bootstrap-servers: kafka:9092

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8090/eureka/